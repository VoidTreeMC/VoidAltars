Duck wants people to only be able to change the boon type after every couple sacrifices
  - We decided to delay this. Players should be able to switch their altar boon as much as they want early on, so they can try out all of the boons and test them

A command that people can use to see recent transactions on their altar, such as who has recently sacrificed to the altar, and what boons have been changed, etc.

Add handling for List<String> constants from config file
Make it so if you remove an entry from the text file and reload, it goes back to the default
Make it so newtown's altar doesnt scale with the number of people
Add a config for loading the item weights in
Add a config for changing the material requirements for altars
Add a config for changing the sacrifice requirements for altars
Add admin command for adding a certain number of sacrifices
Create a way to destroy or move an altar

DID
===
Created a way to move an altar. Should work for destroying it as well
Added permission-handling for who can destroy the altar's interface block
Added an admin command for setting an altar's level
Added an admin command for refreshing an altar's sacrifices
