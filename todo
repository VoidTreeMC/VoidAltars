Add a command that lets administrators reload sacrifices based on the config file
Add a config for changing the material requirements for altars
Make it so if you remove an entry from the text file and reload, it goes back to the default
Make it so newtown's altar doesnt scale with the number of people
Add some type of synchronization to make sure that SQL changes are made synchronously but not on the same thread as the server runtime
Currently, when players unclaim an altar, any subsequent chunks that are unclaimed will tell them their altar has been unclaimed. Until they re-claim it.
Maybe build/destroy transactions should contain the type of altar
Add tab completion for some of the other altar commands besides /altar
Optimize leaderboard code so that it only re-sorts the list if the new score is greater than the score of the one above it
Make /altartop also call /altar top
Altar settings that can be configured by mayor or high priest
- Can outsiders sacrifice
- /altar settings

DID
===
Added server-wide message when a player pleases the gods at an altar
Fixed a bug that caused altars to say they wanted one more sacrifice than they really did
Fixed a bug with /setaltarlevel that caused it to set altars to a lower level but not change their number of sacrifices wanted
Altar leaderboard
- Single leaderboard/category
- Element in top right of GUI that says what rank your altar currently is
- Monthly reset, with season stats
- Calculated from similar formula that determines sacrifice quantities (takes into account quantity, weight, and stack size)
